<div class='container my-5'>
  <div class="d-flex">
    <div class="pb-5">
        <div class="card h-100">
          <div class="card-body">
            <p> <%= cl_image_tag @course.photo.key, height: 200 %></p>
            <h1 class="card-title"><%= @course.name %></h1>
            <%# <p class="card-text"><strong>Descripcion :</strong><%= @course.description %></p> %>
            <%# <p class="card-text"><strong>Precio por dia: </strong> s/. <%= @course.price %></p> %>
              <%= link_to 'Editar', edit_course_path(@course), class: "btn btn-success" %>
              <%= link_to 'Eliminar', course_path(@course), class: "btn btn-primary", method: :delete %>
              <%= link_to 'Rentar', new_course_path(@course), class: "btn btn-primary" %>
          </div>
        </div>
    </div>
  </div>
</div>
